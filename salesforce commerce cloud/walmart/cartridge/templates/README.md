# templates

This folder stores HTML template rendering files and layout structure.
It contains template directories for storefront pages and sections.
In SFRA, templates define server-rendered HTML output for routes.
