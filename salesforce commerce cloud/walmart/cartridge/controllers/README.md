# controllers

This folder defines storefront routes and request handling.
It contains controller `.js` files for endpoints and middleware chains.
In SFRA, controllers map URL requests to business and rendering flows.
