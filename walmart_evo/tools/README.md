# walmart_evo/tools

- `render_timeline_mp4.py` renders `docs/project_timeline_expanded.md` into an MP4 fly-through using only Python stdlib frame generation plus `ffmpeg`.
- Use `--dry-run` to validate parsing on systems where `ffmpeg` is not yet installed.
